import { css } from '@robertakarobin/util/string/template.ts';

// Oddly, stylelint only wants to work in `.css.ts` files when using `css` as opposed to `/*css*/`

export default css`
div {
	color: #000000;
}
`;
